% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adjustment.R
\name{add_covariate_to_scenario}
\alias{add_covariate_to_scenario}
\title{Add a Covariate to an Adjustment Scenario}
\usage{
add_covariate_to_scenario(adjustment_model, scenario_name, variable_name)
}
\arguments{
\item{adjustment_model}{A data frame or tibble representing the adjustment model,
containing at least the columns \code{domain}, \code{scenario}, and \code{variables}.}

\item{scenario_name}{A character string specifying the name of the adjustment scenario
to which the covariate should be added.}

\item{variable_name}{A character string specifying the name of the covariate (variable) to add.}
}
\value{
A modified version of the \code{adjustment_model} with the new covariate added to the specified scenario.
}
\description{
This function adds a specified covariate to a given adjustment scenario within an adjustment model.
}
\details{
The function assumes that the input \code{adjustment_model} contains a \code{domain} column, and the
domain value for the new entry is extracted from the first row of the input model. The new
scenario and variable are appended as a new row to the \code{adjustment_model}.
}
\examples{
# Example adjustment model
adjustment_model <- tibble::tibble(
  domain = "example_domain",
  scenario = c("age_sex", "age_sex"),
  variables = c("age", "sex")
)

# Add a new covariate
updated_model <- add_covariate_to_scenario(
  adjustment_model,
  scenario_name = "age",
  variable_name = "as.factor(SDDSRVYR)"
)

print(updated_model)

}
